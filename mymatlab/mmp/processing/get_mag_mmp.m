function [mag,pr_mag]=get_mag_mmp(drop);%function [mag,pr_mag]=get_mag_mmp(drop);%basic routine just to read the mag data from mmp3.  At this %point it's stupid and user must check to make sure that %specified drop is in fact an mmp3 drop.  Otherwise the file%won't exist and it'll break.  Output are pr_mag, and mag which %has the x,y and z components in nT.%mha 9/10/99%mmpfolderscruise=read_cruises_mmp(drop);fstr2=[procdata FSP cruise FSP 'Mag' FSP 'Mag' ];eval(['load ' fstr2 int2str(drop)]);	if ~exist('pr_mag')	[dum,pr_mag]=get_vac_mmp(drop);end